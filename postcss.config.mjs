const config = {
  // PostCSS plugins configuration
  plugins: {
    // Process Tailwind CSS classes and utilities
    '@tailwindcss/postcss': {},
  },
};

// Export the configuration for PostCSS to use
export default config;
