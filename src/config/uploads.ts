/** Maximum image file size in bytes (5 MB). Shared across all image upload features. */
export const IMAGE_MAX_SIZE = 5 * 1024 * 1024;

/** Accepted MIME types for image uploads. */
export const IMAGE_ACCEPTED_TYPES = ['image/jpeg', 'image/png', 'image/webp'];
